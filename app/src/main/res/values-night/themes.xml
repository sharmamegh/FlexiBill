<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.FlexiBill" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary and secondary colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/cardTextColor</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorSecondaryVariant">@color/colorAccent</item>
        <item name="colorOnSecondary">@color/cardTextColor</item>
        <item name="android:statusBarColor" >@color/colorPrimaryDark</item>
        <item name="android:windowBackground">@color/flexiWallColor</item>
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.FlexiBill" parent="Base.Theme.FlexiBill" />
    <!-- Theme for HomeActivity -->
    <style name="Theme.FlexiBill.HomeActivity" parent="Base.Theme.FlexiBill">
        <item name="android:windowBackground">@color/flexiWallColor</item>
    </style>
    <!-- Theme for BookingConfirmationActivity -->
    <style name="Theme.FlexiBill.BookingConf" parent="Theme.Material3.DayNight.NoActionBar">
    </style>
</resources>